import { styled } from '../../../../../../stitches.config';

export const PaymentListContainer = styled('section', {
    display: 'flex',
    flexWrap: 'wrap',
    gap: '.69rem',
    maxWidth: '16.60938rem'
});